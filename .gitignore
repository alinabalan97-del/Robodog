# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env
.env
.env.*
!.env.example
*.local

# Vite / build caches
.vite/

# 3D assets — ~380 MB of GLBs, served from public/ and copied verbatim into
# dist/. Far too large for git; keep them out of history and distribute them
# alongside the repo. See CLAUDE.md → "Models are sized in METRES".
public/models/
